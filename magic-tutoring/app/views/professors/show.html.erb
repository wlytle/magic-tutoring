<h1><%= @professor.name %></h1>
<div class="prof-stuff">
  <div>
    <%= image_tag (@professor.professor_image), class: "prof-image" %>
    <%= button_to "Edit Profile", edit_professor_path(@professor), class: "button", method: :get if @professor == current_user %>
  </div>
  <div>
    <%= image_tag (@professor.professor_house), class: "house-icon" %>
    <h3>Proficiency: <%= @professor.subject.name %></h3>
    <h3>House: <%= @professor.house %></h3>
    <br>
    <br>
    <br>
    <br>
    <br>
    <br>
    <br>
  </div>
</div>
<%= button_to "Manage Availability", new_professor_availability_path(@professor), class: "button", method: :get if @professor == current_user %>
<br>
<% if current_user == @professor %>
  <h2>Conclaves</h2>
  <br>
  <br>
  <%= render "conclaves/schedule" %>
<% end %>
