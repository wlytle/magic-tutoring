<h1>Subjects offered for Tutelage</h1>
<%= image_tag "subjects/subjects-banner.jpg", class: "banner" %>
<div class="subjects"><ul>
    <% @subjects.each do |subject| %>
    <li><%= link_to subject.name, subject, class: "links" %>
    <% end %>
</ul>
</div>
